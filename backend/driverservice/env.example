PORT=3001
NODE_ENV=development

# PostgreSQL using local trust auth (no password)
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=transtrack_db
DB_SSL=false

# Optional: schema (defaults to public)
# DB_SCHEMA=public

